namespace Organizer.Shared.Enums
{
    public enum JobState
    {
        Pending,
        InProgress,
        Completed,
        Failed,
        Cancelled
    }

    public enum JobType
    {
        FDM,
        /* SLA,
        Markforged,
        SLS */
    }
}
