{
  "scripts": {
    "ui:dev": "npx tailwindcss -c tailwind.config.js -i ./tailwind.input.css -o ./wwwroot/css/app.css --watch",
    "ui:build": "npx tailwindcss -c tailwind.config.js -i ./tailwind.input.css -o ./wwwroot/css/app.css --minify",
    "postinstall": "cd ../Organizer && dotnet run --AppTasks=migrate",
    "migrate": "cd ../Organizer && dotnet run Organizer.csproj --AppTasks=migrate",
    "revert:last": "cd ../Organizer && dotnet run --AppTasks=migrate.revert:last",
    "revert:all": "cd ../Organizer && dotnet run --AppTasks=migrate.revert:all"
  }
}
